# M51I–M51L — Remaining gaps before a live investor demo

1. **Rehearsal on target laptop** — Run full sequence on the exact machine (USB bootstrap / Pane 1 readiness if applicable); confirm `demo mission-control` completes in acceptable time.

2. **Vertical lock** — Lock production cut or active pack before demo so lines 2 and 5 are crisp; otherwise degraded warning is honest but weaker story.

3. **Memory bootstrap depth** — If continuity store is empty, line 3 is thin; optional seed session or narrate “cold start.”

4. **Pane 2 onboarding** — When first-run / memory bootstrap CLIs exist from Pane 2, wire optional stage copy to those commands (additive).

5. **Backup path** — If env `required_ok` is false, have a one-slide verbal fallback (still disclose degraded).

6. **Legal / claims** — Presenter brief: no revenue or deployment claims unless evidence-backed outside this layer.

7. **Optional: persist demo script export** — Single JSON combining session + panel + artifact for audit trail after the meeting.
