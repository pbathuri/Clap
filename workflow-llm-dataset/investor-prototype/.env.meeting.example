# Copy to .env.meeting and adjust. Sourced by scripts/demo-launch-meeting.sh
# EDGE_DESKTOP_REPO_ROOT=/absolute/path/to/workflow-llm-dataset
# EDGE_DESKTOP_PYTHON=/path/to/.venv/bin/python

VITE_EDGE_LIVE=1
VITE_PRESENTER_OVERLAY=1
# After preflight, static JSON loads if API is slow:
VITE_EDGE_STATIC_SNAPSHOT=/edge-desktop-snapshot.json
